<!--When using form_with helper 
     1. Use (model:) so that the form is pre-populated with teh existing object's data (since this is an edit action)
     2. It nomally uses ajax to submit the form.  To override that with 
        a simple Get request:  local: true -->
<form>
  <div class="container justify-content-center col-12">
    <%= form_with(model: @post, local: true, 
                  class: "shadow p-3 mb-3 bg-grey rounded")  do |f| %>

      <div class="row justify-content-md-center">
        <div class="col-8">
          <div class="card text-left shadow bg-white rounded">
            <h5 class="card-body">
              <%= f.label "Title: " %>
              <%= f.text_field :title, class: "form-label shadow rounded col-10",
                                placeholder: "Enter a title" %>
            </h5>

            <div class="card shadow rounded">
              <%= f.text_area :body, rows: 15, placeholder: "Enter body here" %>
            </div>
            
          </div>
        </div>
      </div>
      <div class="text-center mt-2 mb-2">
        <%= f.submit %>
      </div>

    <% end %>
  </div>
</form>